# 项目协作规则

除非用户明确要求，否则遵守以下规则：

1. 不破坏或污染本地环境；运行、安装、构建和测试优先使用 Docker 或其他隔离环境。
2. 代码只保留必要、清晰且有价值的注释。
3. 文档保持简洁、准确、可执行。
4. 变更以合理性为优先，不要求历史兼容，但必须明确提醒用户不兼容影响。
5. 任务完成并验证后，允许自动执行 `git commit` 和 `git push`，无需再次确认；提交作者必须为 `Euphie <euphie@vip.qq.com>`。提交前检查变更范围，默认只推送当前分支，不得自动强制推送或改写远程历史。
6. 不提交 AI 或过程文件，包括 `.superpowers/`、`docs/superpowers/`、`.openai/` 及同类临时产物。
7. 较小的需求和任务不使用 superpowers。
